@model IEnumerable<GNews.Models.Client>

@{
    ViewBag.Title = "Index";
}

<h2>Gestionar Clientes</h2>

<p>
    @Html.ActionLink("Añadir cliente", "Create",null, new { @class = "btn btn-default" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientName)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ClientID }, new { @class = "btn btn-default btn-xs" }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ClientID }, new { @class = "btn btn-primary btn-xs" }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.ClientID }, new { @class = "btn btn-danger btn-xs" })
        </td>
    </tr>
}

</table>
